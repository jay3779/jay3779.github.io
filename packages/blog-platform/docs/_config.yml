# GitHub Pages Configuration

# Build settings
markdown: kramdown
theme: null  # We're using custom HTML/CSS

# Exclude unnecessary files
exclude:
  - .github
  - scripts
  - posts
  - package.json
  - package-lock.json
  - node_modules
  - README.md
  - .gitignore
